plugins {
    // Auto-provisions the JDK toolchain declared in build.gradle, so contributors
    // don't need a matching JDK installed locally.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'Chatty'
